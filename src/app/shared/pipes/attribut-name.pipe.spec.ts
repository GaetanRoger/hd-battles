import { AttributNamePipe } from './attribut-name.pipe';

describe('AttributNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AttributNamePipe();
    expect(pipe).toBeTruthy();
  });
});
